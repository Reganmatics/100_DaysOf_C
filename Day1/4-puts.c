#include <stdio.h>
/**
 * main - function entry point
 * @void: carries no parameters
 * Description: outputs the specified string
 * Return: 0 if succesful
 **/

int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
