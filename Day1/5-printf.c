#include <stdio.h>

/**
 * main - function entry point
 * Description: displays the text within the `string quotes`
 * Return: always 0 for a succesful run
 */


int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
